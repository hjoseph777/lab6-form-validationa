# Check your current remote URL
git remote -v

# Remove the incorrect remote
git remote remove origin

# Add the correct remote
git remote add origin https://github.com/hjoseph777/lab6-form-validationa.git

# Verify the change
git remote -v
